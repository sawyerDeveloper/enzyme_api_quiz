const QuizComplete = (props) => {

const styles = {
    container: {
        fontSize: 200,
        color: 'purple'
    }
}

    return(
        <div style={styles.container}>
            Quiz Complete!
        </div>
    )
}

export default QuizComplete